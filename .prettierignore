# build output and dependencies
node_modules/
dist/
pnpm-lock.yaml
# the desktop app dir + install image electron-builder packs (scripts/stage.mjs)
packages/desktop/stage/

# docs-only area (mostly Chinese; not machine-formatted)
specs/
*.md

# Playwright e2e artifacts
test-results/
playwright-report/
